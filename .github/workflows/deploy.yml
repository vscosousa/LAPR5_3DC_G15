name: .NET CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '8.0.x'

    - name: Restore dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build --no-restore

    - name: Run tests and generate coverage report
      run: dotnet test --no-build --collect:"XPlat Code Coverage" --results-directory ./TestResults

    - name: Publish code coverage
      uses: actions/upload-artifact@v3
      with:
        name: code-coverage
        path: './TestResults/**/coverage.cobertura.xml'

    - name: Commit coverage report
      if: success()
      env:
        PAT: ${{ secrets.PAT }}
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'
        git checkout -b coverage-report
        mkdir -p coverage
        cp ./TestResults/**/coverage.cobertura.xml coverage/
        git add coverage/coverage.cobertura.xml
        git commit -m "Add code coverage report"
        git push https://x-access-token:${{ secrets.PAT }}@github.com/${{ github.repository }} coverage-report